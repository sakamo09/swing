<main class="main">
  <div class="inner">
    <%# <% if user_signed_in? %> 
      <div class="greeting">
        ようこそ、
        <%# <%= link_to "#{current_user.name}さんの", user_path(current_user), class: :greeting__link%> 
        お気に入りのバドミントン商品を紹介してね！
      </div>
    <%# <% end %> 
    <div class="card__wrapper">
      <%# <%= render partial: 'prototype', collection: @prototypes %> 
    </div>
  </div>
</main>
